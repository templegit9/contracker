/**
 * Auth UI component - Placeholder for backward compatibility
 * No longer needed with session-based system
 */

export function initAuthUI() {
    console.log('Auth UI: Using session-based system, no login required');
    // No-op - session system handles everything in app.js
}
